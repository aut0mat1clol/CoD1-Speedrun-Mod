// Speedrun mod autoexec (engine reads this once at start).
// rt_cont_real/rt_cont_wall: archived continuity channel (CVAR_ARCHIVE) -
// the GSC refreshes them every tick; do not edit by hand.
// rt_dll_api is managed ONLY by install.ps1 -Patch / -Revert:
// 14 = the dll provides the native velocity api (exact speedo) AND the
// wall clock (pause counting). 0 = pure script mode. No hand edits.
seta rt_cont_real "0"
seta rt_cont_wall "0"
seta rt_dll_api "0"
